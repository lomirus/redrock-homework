```c
void (*signal(int sig,void (*func)(int)))(int);
//func是一个函数指针，指向一个[参数为整型、返回值为void]的函数；
//同时[函数指针func（所指向的函数）和整数型sig]共同作为参数传递给函数指针signal（所指向的函数）；
//而函数指针signal（所指向的的函数）的返回值为一个[要求整数型参数，返回值类型为void]的函数；
```
~~ 梦回语文高考之“长句化短句” ~~